{
  "name": "karta-potencialu",
  "version": "1.0.0",
  "private": true,
  "description": "Karta potenciálu nad Raynetem pro RENOVA, s.r.o. — segmentově řízené karty obchodního potenciálu.",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,web -c magenta,cyan \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server run start",
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "check:ds": "node tools/check-design-system.mjs web/src"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
